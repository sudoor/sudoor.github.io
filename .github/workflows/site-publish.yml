name: site-publish
on:
  push:
    schedule:
      - cron: '0 17 * * *'
    branches:
      - master
jobs:
  npm-publish:
    name: npm-publish
    runs-on: ubuntu-latest
    steps:
    - name: Build
      run: bash ./deploy.sh
